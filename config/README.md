# Configuration

Configuration files for SmartStock AI.

- `settings.yaml` - Application settings
- `database.yaml` - Database configuration
- `ml_config.yaml` - ML pipeline configuration
- `model_config.yaml` - Model hyperparameters
